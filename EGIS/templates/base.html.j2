<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>EGIS Vulnerability Report - {{ org_name }}</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 20px;
        }
        .header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 30px;
        }
        .title {
            font-size: 24px;
            font-weight: bold;
        }
        .org-name {
            font-size: 20px;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 20px;
        }
        th, td {
            border: 1px solid #ddd;
            padding: 8px;
            text-align: left;
        }
        .stats-container {
            display: flex;
            justify-content: center;
            gap: 30px;
            margin: 40px 0;
        }
        .hexagon {
            position: relative;
            width: 60px;
            height: 70px;
            text-align: center;
            color: white;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
        }
        .critical { background-color: #8b60c7; }
        .high { background-color: #c25555; }
        .medium { background-color: #e69138; }
        .low { background-color: #45818e; }
        .vuln-card {
            background-color: #ffebee;
            border: 1px solid #ffcdd2;
            border-radius: 4px;
            padding: 15px;
            margin-bottom: 20px;
        }
        .tlp-red {
            color: red;
            text-align: right;
            font-weight: bold;
            margin-top: 20px;
        }
        .ports-table {
            font-size: 14px;
        }
        .ports-table td {
            padding: 4px 8px;
        }
    </style>
</head>
<body>
    {% block content %}{% endblock %}
    <div class="tlp-red">TLP:RED</div>
</body>
</html>
